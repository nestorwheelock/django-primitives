# django-layers

Import boundary enforcement for Django monorepos.

## Install

```bash
pip install django-layers
```

## Quick Start

1. Add `django_layers` to `INSTALLED_APPS`
2. Configure layer boundaries in settings
3. Run the layer audit to detect violations

## Versioning

This package follows semantic versioning. See `__version__` in the package root.
