"""Django app configuration for django-documents."""

from django.apps import AppConfig


class DjangoDocumentsConfig(AppConfig):
    """App configuration for django-documents."""

    name = 'django_documents'
    verbose_name = 'Django Documents'
    default_auto_field = 'django.db.models.BigAutoField'
