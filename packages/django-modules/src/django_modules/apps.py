"""Django app configuration for django-modules."""

from django.apps import AppConfig


class DjangoModulesConfig(AppConfig):
    """App config for django-modules."""

    name = "django_modules"
    verbose_name = "Django Modules"
    default_auto_field = "django.db.models.BigAutoField"
