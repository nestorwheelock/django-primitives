"""Django app configuration for django-questionnaires."""

from django.apps import AppConfig


class DjangoQuestionnairesConfig(AppConfig):
    """Configuration for django-questionnaires app."""

    name = "django_questionnaires"
    verbose_name = "Questionnaires"
    default_auto_field = "django.db.models.BigAutoField"
